const initialState = {
  currency: 'BRL',
  field: 0,
};

const wallet = (store = initialState, action) => {
  switch (action.type) {
  default:
    return { ...store };
  }
};

export default wallet;
